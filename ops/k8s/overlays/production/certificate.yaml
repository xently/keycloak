apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak-certificate
  namespace: keycloak
spec:
  secretName: keycloak-tls-secret
  commonName: oauth.xently.co.ke
  dnsNames:
    - oauth.xently.co.ke
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
